import React from 'react';
import MainCarousel from './MainCarousel';
import Products from './Products';

const Home = (props) => {
  return (
    <>
      <MainCarousel />
      <Products />
    </>
  );
};

export default Home;
